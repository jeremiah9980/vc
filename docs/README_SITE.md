# Site Notes

This folder is ready for GitHub Pages.

## Publish methods

### Simple branch publish
- Push this repo to GitHub
- Open Settings → Pages
- Choose the `main` branch and `/docs` folder

### GitHub Actions publish
Use the included workflow in `.github/workflows/deploy-pages.yml`.
